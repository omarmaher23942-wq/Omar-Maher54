# Images

- Place your **profile photo** here as `omar-maher.jpg` (recommended size: 400×400 or larger, square).
- Optional: add project images in `projects/` (e.g. `shoghlana.jpg`, `meta-support.jpg`) — see `backend/data/projects.py` for image paths.
